# Screenshots

Place four PNGs here:

- `01-canvas-overview.png` — full workflow canvas, showing all six sections
  (source, detect, enrich, qualify, deliver)
- `02-github-search-node.png` — GitHub Code Search node expanded, showing
  the query parameter and header auth
- `03-claude-generation-node.png` — Claude Generate Email node expanded,
  showing the system prompt and user message template
- `04-sheet-output.png` — the resulting Google Sheet with a few sample
  leads populated

Screenshots are omitted from the repo until the workflow has been imported
into a running n8n instance. Reviewers viewing the raw JSON can re-create
the canvas by importing `workflow/github-signal-outbound.json`.
